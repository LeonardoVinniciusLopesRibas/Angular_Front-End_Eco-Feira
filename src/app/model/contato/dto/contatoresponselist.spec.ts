import { Contatoresponselist } from './contatoresponselist';

describe('Contatoresponselist', () => {
  it('should create an instance', () => {
    expect(new Contatoresponselist()).toBeTruthy();
  });
});
